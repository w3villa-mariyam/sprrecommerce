<%= form_with(model: product_option_type) do |form| %>
  <% if product_option_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_option_type.errors.count, "error") %> prohibited this product_option_type from being saved:</h2>

      <ul>
        <% product_option_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :option_type_id %>
    <%= form.number_field :option_type_id %>
  </div>

  <div class="field">
    <%= form.label :product %>
    <%= form.text_field :product %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
