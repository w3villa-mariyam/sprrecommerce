<%= form_with(model: shipping_category) do |form| %>
  <% if shipping_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping_category.errors.count, "error") %> prohibited this shipping_category from being saved:</h2>

      <ul>
        <% shipping_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
